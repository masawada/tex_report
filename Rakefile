# TeX Report Template - Rakefile

# loader
def require_dir(dir)
  Dir.glob("#{__dir__}/#{dir}/*.rb").each { |f| require f }
end

TEX = "tex"
TMP = "tmp"
OUT = "out"
CONFIG = "config"
LIB = "lib"
CODES = "src"
IMAGES = "images"

require_dir 'lib/tasks'
